# Test-only overrides, applied on top of the packaged application.properties.
# The visit confirmation secret is deliberately absent from the main configuration so
# that a deployment must supply it; the tests use this fixed value instead.
petclinic.visit-token-secret=test-only-visit-token-secret
